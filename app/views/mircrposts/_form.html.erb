<%= form_for(@mircrpost) do |f| %>
  <% if @mircrpost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mircrpost.errors.count, "error") %> prohibited this mircrpost from being saved:</h2>

      <ul>
      <% @mircrpost.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
